<tb:TaskbarIcon x:Class="TimeLogger.Wpf.Views.Other.MainTray"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:TimeLogger.UserInterface.ViewModels;assembly=TimeLogger.UserInterface"
        ToolTip="Time Logger"
        IconSource="pack://application:,,,/TimeLogger;component/timeicon.ico"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:TaskTrayViewModel}"
        PreviewTrayContextMenuOpen="MainTray_OnPreviewTrayContextMenuOpen"
    >
    <tb:TaskbarIcon.ContextMenu>
        <ContextMenu ItemsSource="{Binding MenuItems}">
            <ContextMenu.ItemTemplate>
                <DataTemplate>
                    <MenuItem Header="{Binding Header}" Command="{Binding Command}" />
                </DataTemplate>
            </ContextMenu.ItemTemplate>    
        </ContextMenu>
    </tb:TaskbarIcon.ContextMenu>
</tb:TaskbarIcon>
